# Workflow Rules

- For changes touching multiple files or behaviors, start with a brief design summary and assumptions.
- Write an implementation plan with small, reviewable steps; each step should be independently verifiable.
- Execute in small batches and validate after each batch instead of one large final pass.
- Final handoff must include:
  - What changed
  - How it was verified
  - Remaining risks or follow-up items

