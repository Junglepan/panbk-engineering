# Security Rules

- Never print or persist credentials in generated code, logs, or examples.
- Prefer least-privilege changes: grant only the permissions needed for the requested task.
- For risky operations (destructive commands, production-impacting edits, credential access), require explicit confirmation.

